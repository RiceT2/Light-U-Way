server:
  port: 8080

spring:
  application:
    name: xiaoyouxing
  datasource:
    url: jdbc:mysql://localhost:3306/xiaoyouxing?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: xiaoyou
    password: xiaoyou1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  elasticsearch:
    uris: http://localhost:9200
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:demo-key}
